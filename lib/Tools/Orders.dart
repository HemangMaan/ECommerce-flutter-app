class Orders {
  String prodName, quantity, price, address, customerName, uid;

  Orders(
      {this.uid,
      this.prodName,
      this.quantity,
      this.price,
      this.address,
      this.customerName});
}
